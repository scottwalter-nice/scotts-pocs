import { SeeMoreDirective } from './see-more.directive';

describe('SeeMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new SeeMoreDirective();
    expect(directive).toBeTruthy();
  });
});
