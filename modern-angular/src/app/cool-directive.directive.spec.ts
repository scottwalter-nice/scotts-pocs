import { CoolDirectiveDirective } from './cool-directive.directive';

describe('CoolDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
