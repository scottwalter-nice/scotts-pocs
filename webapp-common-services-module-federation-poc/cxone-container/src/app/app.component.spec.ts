//Remove this test once you have real tests in your app
//The sample tes is required to get karma to pass out of the box
describe('Sample Test', () => {
  it('should do something', () => {
      expect(true).toBeTruthy();
  });
});